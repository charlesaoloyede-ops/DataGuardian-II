import 'package:flutter/material.dart';

/// App Usage screen — Phase 5 implementation.
class AppUsageScreen extends StatelessWidget {
  const AppUsageScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('App Usage')),
      body: const Center(child: Text('App Usage — coming in Phase 5')),
    );
  }
}
