import 'package:flutter/material.dart';

/// Alert Configuration — Phase 5 implementation.
class AlertConfigScreen extends StatelessWidget {
  const AlertConfigScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Alert Settings')),
      body: const Center(child: Text('Alert Config — coming in Phase 5')),
    );
  }
}
