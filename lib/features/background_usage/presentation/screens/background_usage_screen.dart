import 'package:flutter/material.dart';

/// Background Usage screen — Phase 5 implementation.
class BackgroundUsageScreen extends StatelessWidget {
  const BackgroundUsageScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Background Usage')),
      body: const Center(child: Text('Background Usage — coming in Phase 5')),
    );
  }
}
